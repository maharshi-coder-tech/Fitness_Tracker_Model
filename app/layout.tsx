import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
})

export const metadata: Metadata = {
  title: "AI Fitness Tracker - Personalized Fitness & Nutrition",
  description:
    "Get AI-powered personalized fitness and nutrition recommendations based on advanced machine learning models with 93% accuracy.",
  keywords: "AI fitness, machine learning, personalized nutrition, workout plans, XGBoost, fitness tracker",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className={`${inter.className} antialiased`}>{children}</body>
    </html>
  )
}
